node_modules
dist
*.log
.DS_Store
.vercel
tsconfig.tsbuildinfo
.env
.env.local
.env\ -\ Copy*

# Large asset source files (AI/EPS/PSD/TIFF/SCML) — not needed at runtime
*.ai
*.eps
*.psd
*.tiff
*.scml

# Large zip/archive files — not needed in repo
*.zip
*.7z
*.rar
assets/animationsweapons.zip

# Reference assets — screenshots, rigs preview, planning docs (not runtime)
assets/*.png
assets/extw-rigs-preview/
assets/needs pipeline and best practices/
review and organize/

# Maya source files (not needed at runtime)
*.ma
*.mb

# Server backend
server/node_modules
server/dist
server/.env
server/.env.local
